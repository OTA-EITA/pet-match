apiVersion: v1
kind: Secret
metadata:
  name: petmatch-secrets
  namespace: petmatch
type: Opaque
stringData:
  # Redis Auth
  REDIS_PASSWORD: "petmatch123"
  
  # JWT Secrets
  JWT_ACCESS_SECRET: "dev-access-secret-key-please-change-in-production"
  JWT_REFRESH_SECRET: "dev-refresh-secret-key-please-change-in-production"
  
  # MinIO Credentials (for future use)
  MINIO_ACCESS_KEY: "minioadmin"
  MINIO_SECRET_KEY: "minioadmin"
  
  # External API Keys (mock for development)
  FCM_SERVER_KEY: "mock-fcm-server-key"
  GOOGLE_MAPS_API_KEY: "mock-google-maps-api-key"
